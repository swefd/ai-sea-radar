// Ручні декларації: tsconfig має allowJs: false, тож імпорт цього .mjs із .spec.ts —
// TS7016. Тримати синхронно з експортами руками; розбіжність ловить `check-types`.
export type VersionMap = Record<string, string>;

export declare const ALLOWED: {
  dependencies: VersionMap;
  devDependencies: VersionMap;
};

/**
 * Вхід — `unknown`, а не `Record<string, unknown>`, і це навмисне відхилення від
 * брифа: маніфест приходить із `JSON.parse`, тобто типом не є нічим, а варта на
 * не-об'єкт усередині функції — частина її контракту, а не оборона понад нього.
 * З вужчим оголошенням тест на цю варту довелося б писати через каст, тобто
 * документувати найважливішу гілку як порушення типів.
 */
export declare function checkManifest(manifest: unknown): string[];

export interface DenyRule {
  rule: string;
  why: string;
  names: string[];
  scopes: string[];
}

/**
 * Експортується заради двох тестів, і вони пара. «перетин ALLOWED і DENIED порожній»
 * стереже ПЕРЕДУМОВУ: на справжньому наборі пріоритет між списками не спостережний,
 * поки перетин порожній. «колізія ALLOWED×DENIED» вносить перетин усередині тесту на
 * час одного твердження і знімає його у `finally` — так сама гілка пріоритету теж
 * отримує варту, а набір лишається цілим.
 *
 * Тут стояло, що іншого способу спостерегти пріоритет НЕМАЄ. Це виміряно хибне, і
 * абсолют знято: розворот пріоритету не червонив жодного тесту (0438273 — 115/0),
 * з другим тестом червонить рівно один (115/1).
 * Твердження про неможливість коштує дорожче за помилкове число — число бреше про
 * одну величину й запрошує перевірити, «іншого способу немає» бреше про весь простір
 * способів і зупиняє пошук.
 */
export declare function deniedRule(name: string): DenyRule | undefined;
