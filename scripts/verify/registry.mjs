// Реєстр — ДАНІ. Жодної логіки: щоб додати перевірку, цей файл дописують,
// а run.mjs не чіпають. Якщо для нового рядка довелося правити run.mjs — межа зламана.
import { existsSync, readdirSync } from 'node:fs';
import { createRequire } from 'node:module';
import { homedir, platform } from 'node:os';
import path from 'node:path';

/** Дефолтний таймаут одного рядка. Рядок може перекрити своїм timeoutMs. */
export const DEFAULT_TIMEOUT_MS = 120_000;

/**
 * PRECONDITIONS — мапа id → {describe, probe}.
 * Проба відповідає ЛИШЕ на «чи можу я взагалі запуститися», ніколи на «чи пройшло».
 */
export const PRECONDITIONS = {
  'node-modules': {
    describe: 'немає node_modules/ — залежності не встановлені',
    probe: (root) => existsSync(path.join(root, 'node_modules')),
  },
  'playwright-pkg': {
    describe: 'не резолвиться @playwright/test',
    probe: (root) => {
      try {
        createRequire(path.join(root, 'package.json')).resolve('@playwright/test');
        return true;
      } catch {
        return false;
      }
    },
  },
  'playwright-browser': {
    describe: 'немає бінарника chromium у кеші Playwright',
    probe: (root) => {
      try {
        return readdirSync(browsersPath(root)).some((entry) => entry.startsWith('chromium'));
      } catch {
        return false;
      }
    },
  },
};

function defaultBrowsersPath() {
  const home = homedir();
  if (platform() === 'darwin') return path.join(home, 'Library', 'Caches', 'ms-playwright');
  if (platform() === 'win32') return path.join(home, 'AppData', 'Local', 'ms-playwright');
  return path.join(home, '.cache', 'ms-playwright');
}

/**
 * Куди Playwright кладе браузери. `PLAYWRIGHT_BROWSERS_PATH=0` — документоване
 * значення «у node_modules», і воно істинне як рядок: наївне `env || default`
 * робить із нього відносний шлях «0», проба падає й вічно каже «браузера немає».
 * Тихий SKIPPED там, де e2e цілком міг би бігти. Розвʼязання — дослівно як у
 * playwright-core: при "0" це `<корінь пакета playwright-core>/.local-browsers`.
 * Гілка відносного шляху — свідоме наближення, а не копія: playwright рахує його
 * від `INIT_CWD || process.cwd()`, ми — від `root`.
 */
export function browsersPath(root) {
  const fromEnv = process.env.PLAYWRIGHT_BROWSERS_PATH;
  if (fromEnv === '0') {
    const requireFrom = createRequire(path.join(root, 'package.json'));
    return path.join(path.dirname(requireFrom.resolve('playwright-core/package.json')), '.local-browsers');
  }
  // Відносне значення playwright рахує від `INIT_CWD || process.cwd()` процесу,
  // який запущено з `cwd: root`. Раннер же резолвив би його від власної cwd, а за
  // R-20 це інша тека. `INIT_CWD` тут не відтворюємо: у кожному шляху, який створює
  // цей шар, він дорівнює `root`, а точний збіг означав би переписати змінну npm
  // усередині проби, що відповідає на одне «так/ні».
  if (fromEnv) return path.resolve(root, fromEnv);
  return defaultBrowsersPath();
}

export const CHECKS = [
  {
    id: 'typecheck',
    tier: 'fast',
    // Команда живе в package.json в однині — інакше реєстр і розробник
    // запускали б різні tsc і розходилися непомітно.
    cmd: 'npm run --silent check-types',
    needs: ['node-modules'],
    after: [],
    proves: 'Кожен `.ts`/`.tsx` проєкту компілюється під `strict` із типами Next',
    // Доповнення контролера (R-58). Перевірено інструментом, а не читанням
    // конфігу: `tsc -p tsconfig.json --noEmit --listFilesOnly` друкує три
    // `.d.mts` і жодного `.mjs`. Тобто рядок, який називається `typecheck`,
    // про код самої перевірки не знає нічого — а це той код, чия мовчазна
    // помилка робить зеленим усе решту. Спека §3.1 забороняє blindSpot,
    // вужчий за реальність.
    blindSpot: 'Нічого про поведінку в рантаймі. `skipLibCheck: true` ховає помилки чужих `.d.ts`. Leaflet не виконується, тож звернення до `window` на рівні модуля тут скомпілюється успішно. Код самого шару не дивиться зовсім: за `allowJs: false` жоден `scripts/**/*.mjs` і жоден `.claude/hooks/**/*.mjs` не потрапляє в програму TypeScript. Сусідні `.d.mts` написані руками й ніколи не звіряються зі своїм `.mjs`: оголошення може розійтися з реалізацією або бути вигадкою повністю, а `check-types` лишиться зеленим',
  },
  {
    id: 'lint',
    tier: 'fast',
    cmd: 'npm run --silent lint',
    needs: ['node-modules'],
    after: [],
    proves: 'Жоден файл не порушує правила `eslint-config-next` і базові правила TS',
    // Доповнення контролера (R-51). Перевірено не читанням конфігу, а через
    // `eslint --print-config scripts/verify/hash.mjs`: у злитих globals присутні
    // window, document, localStorage і navigator, а `no-undef` має рівень 2.
    // Тобто єдиний рядок, яким шар охороняє власні .mjs, для цілого класу імен
    // мовчазно неробочий. Спека §3.1 забороняє blindSpot, вужчий за реальність.
    //
    // Доповнення контролера (R-59). Перевірено інструментом: файл із
    // невизначеним іменем, покладений у `.superpowers/**`, дає від ESLint
    // «File ignored because of a matching ignore pattern», а `npm run lint`
    // по всьому репозиторію лишається тихим і віддає 0. Перелік — у
    // `eslint.config.mjs:7-27`.
    blindSpot: 'Стиль і статичні шаблони, не логіку. Правило, якого немає в конфігу, не порушується за визначенням. `no-undef` увімкнено лише для `scripts/**/*.mjs` і `.claude/hooks/**/*.mjs`, і навіть там `eslint-config-next` віддає цим файлам ще й браузерний набір глобалей — тож `window`, `document` чи `localStorage`, помилково вжиті в серверному скрипті, не позначаються ніколи. Периметр задає явний список ігнорів, а не дерево репозиторію: `reference/**`, `.verify/**`, `.claude/worktrees/**`, `.superpowers/**` і вивід збірки не лінтуються взагалі. Flat-конфіг не читає `.gitignore`, тож цей список — єдине, що визначає межу',
  },
  {
    id: 'unit',
    tier: 'fast',
    cmd: 'npx playwright test --project=unit',
    needs: ['node-modules', 'playwright-pkg'],
    after: [],
    // Нуль знайдених тестів — SKIPPED «0 тестів написано», а не PASSED (спека §3.2).
    emptyProbe: {
      cmd: 'npx playwright test --project=unit --list',
      reason: '0 тестів написано',
    },
    proves: 'Чисті функції поводяться як задано — для написаних випадків',
    // Доповнення контролера (R-52): «ні браузера» — домовленість тестів, а не
    // заборона в конфігу. Проєкт `unit` не задає жодного бар'єра, який завадив би
    // спеці звернутися до `page` і підняти chromium. Спека §3.1 забороняє
    // blindSpot, вужчий за реальність, тож формулювання каже, що саме тримає межу.
    //
    // Доповнення контролера (R-62). Відтворено, а не виведено з конфігу: файл з
    // одним `test.only` дає `--list` → «Total: 3 tests», прогін → «1 passed» і
    // вихід 0; файл із суцільним `test.skip` дає «2 skipped» і вихід 0. Обидва —
    // PASSED. Проба порожнечі тут не рятує: вона питає, чи тести НАПИСАНО, а
    // діра в тому, що написані тести не ВИКОНАНО.
    blindSpot: 'Ні DOM, ні Leaflet. Браузера не піднімає — але це домовленість тестів, а не заборона в конфігу: спека, яка звернеться до `page`, підніме chromium і в цьому проєкті. Про рендер і карту не говорить нічого. Випадок, якого ніхто не написав, не покритий. Написаний, але не виконаний випадок — теж не покритий: `test.only` за невимкненого `forbidOnly`, суцільний `test.skip` або звуження через `--grep` лишають вихід 0, а проба переліку рахує такі тести як наявні, тож `emptyProbe` не спрацьовує. Рядок покаже PASSED прогону, в якому не виконано жодного твердження',
  },
  {
    id: 'no-ref-imports',
    tier: 'fast',
    cmd: 'node scripts/verify/checks/no-ref-imports.mjs',
    // Порожній needs — свідомо: перевірці не треба ні node_modules, ні браузера,
    // лише git і сам Node. За R-72 неохоронений рядок мовчки зеленіє, якщо його
    // команда падає тихо, тож `main()` тут виходить ненульовим кодом і друкує
    // причину, а не лишає порожній stdout.
    needs: [],
    after: [],
    // Опис МЕХАНІЗМУ, а не перелік написань. Перелік — нескінченна поверхня для
    // перебільшення: щоразу знаходилося ще одне написання, якого сканер не ловить,
    // і твердження ставало неправдивим три раунди поспіль.
    proves:
      'Жоден файл із периметра свіжості (`hash.mjs:isSourcePath`) з розширенням коду '
      + 'або `.css` не містить специфікатора В ЛАПКАХ, у шляху якого є сегмент '
      + '`reference`, у жодній із форм, які впізнає `FORMS` самої перевірки, — ні в '
      + 'рантаймі, ні на рівні типів, ні в багаторядковій формі, ні коли між ключовим '
      + 'словом і `from` стоїть коментар із крапкою з комою — крім написання з '
      + 'вкладеним шаблоном на тому самому рядку, названого в `blindSpot`',
    // Спека §4 дає перші два речення дослівно. Решта дописана тому, що §3.1 забороняє
    // blindSpot вужчий за реальний пропуск, а ці діри реальні.
    //
    // Кожна названа тут діра ПРИШПИЛЕНА тестом у `no-ref-imports.spec.ts`, який
    // стверджує, що діра є. Це не закріплення слабкості: закриють діру — тест
    // почервоніє й змусить оновити цей рядок. Інакше цю прозу не здатен спростувати
    // жоден тест, і вона дрейфує (виміряно: дрейфувала три раунди поспіль).
    blindSpot:
      'Тільки статичні літерали шляхів У ЛАПКАХ. `@import url(…)` без лапок — валідний '
      + 'CSS — проходить мовчки. Шлях, зібраний обчисленням у рантаймі, невидимий. '
      + 'Аліас у `paths` з `tsconfig.json`, що вказує на `reference/`, дав би імпорт, '
      + 'якого цей сканер не бачить. `/// <reference types="…" />` проходить мовчки: '
      + 'ловиться лише варіант із `path=`. Файли, ігноровані git-ом, не скануються. '
      + 'Периметр — це периметр хеша свіжості, звужений за розширенням, і звуження '
      + 'реальне: `.json`, `.scss`, `.nvmrc`, `.md` і будь-яке інше розширення поза '
      + 'списком не читаються взагалі, хоча в ключ свіжості входять. '
      + 'Сам периметр розійтися з хешем більше не може — це один і той самий '
      + '`isSourcePath`, а не два узгоджені списки. '
      + 'Число порушень уміє занижувати: ключ дедуплікації — рядок плюс специфікатор, '
      + 'тож два порушення з тим самим шляхом, що починаються на ОДНОМУ рядку, дають '
      + 'один рядок звіту. Вердикт від цього не міняється ніколи (нуль проти '
      + 'не-нуля), лише N. '
      + 'У зворотний бік сканер свідомо перебирає: він не парсить мову, тож проза, яка '
      + 'саме правило ЦИТУЄ, здатна дати хибне спрацювання — і закоментований імпорт '
      + 'теж рахується порушенням навмисно. Це вибір «перебір безпечний, недобір — '
      + 'ні», а не недогляд. '
      + 'Ціна того ж вибору — номер рядка: збіг починається від ключового слова, тож '
      + 'зайвий `import` у коментарі вище, без крапки з комою між ними, здатен '
      + 'приписати справжнє порушення своєму рядку, а не рядку імпорту. '
      + 'Забілення коментарів для другого проходу розуміє рядкові літерали й шаблони, '
      + 'але не мову: вкладений шаблон із бектиком усередині `${…}` здатен спарувати '
      + 'лапки не так, і тоді маркер, що стоїть у літералі, читається як код. Недобір '
      + 'із цього виходить лише тоді, коли на ОДНОМУ рядку стоять і таке написання, і '
      + 'початок багаторядкового імпорту; у периметрі вкладених шаблонів нема. Решта '
      + 'хиб розбору літералів помиляється в протилежний бік — забілює МЕНШЕ, а прохід '
      + 'по сирому тексту лишається підлогою, тож хибного PASSED з них не виходить',
  },
  {
    id: 'build',
    tier: 'full',
    cmd: 'npm run --silent build',
    needs: ['node-modules'],
    after: ['typecheck'],
    timeoutMs: 300_000,
    proves: 'Застосунок збирається і серверний рендер не звертається до `window` на рівні імпорту — запобіжник SSR для Leaflet',
    blindSpot: 'Нічого не натискає. Карта, що вийшла сірою, збірку проходить',
  },
  {
    id: 'e2e',
    tier: 'full',
    cmd: 'npx playwright test --project=e2e',
    needs: ['node-modules', 'playwright-pkg', 'playwright-browser'],
    after: ['build'],
    timeoutMs: 300_000,
    emptyProbe: {
      cmd: 'npx playwright test --project=e2e --list',
      reason: '0 тестів написано',
    },
    proves: 'Поведінки вибору з B-07 виконуються у справжньому chromium проти dev-сервера',
    // Перші два речення — дослівно зі спеки §4. Третє додано свідомо:
    // §3.1 забороняє blindSpot, вужчий за реальність, а reuseExistingServer: true
    // з конфігу задачі 1 дозволяє протестувати чужий сервер на 127.0.0.1:3000.
    blindSpot: '**Рух не перевіряє** — перевірка руху з керованим часом належить R3. Тайли заблоковані, тож про справжні зображення карти не говорить нічого. Не доводить, що dev-сервер зібрано саме з цього дерева: `reuseExistingServer: true` прийме вже піднятий сервер іншої гілки. Те саме, що й у `unit`: `test.only` за невимкненого `forbidOnly` чи суцільний `test.skip` дають вихід 0 і PASSED без жодного виконаного твердження',
  },
];
