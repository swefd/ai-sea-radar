---
name: verify
description: Шар перевірки Sea Radar — коли запускати `npm run verify` / `verify:full` / `verify:checkpoint`, як читати п'ять статусів звіту, що роблять два хуки і чого шар свідомо не доводить. Use when running or reading this project's verification layer, when the Stop gate blocks a turn, when a check reports FAILED / SKIPPED / NOT_RUN / UNRUNNABLE, or before claiming that work is done, green, verified or ready to hand over.
---

# Шар перевірки

Живе у `scripts/verify/` (реєстр, раннер, звіт, самі перевірки) і в `.claude/hooks/`
(два хуки, які їх кличуть). **Реєстр — дані, не скрипти:** щоб додати перевірку,
дописують `scripts/verify/registry.mjs`, а `run.mjs` не чіпають. Якщо для нового рядка
довелося правити `run.mjs` — межа зламана.

Вісім рядків реєстру: `typecheck`, `lint`, `unit`, `no-ref-imports`, `no-secrets`,
`deps-allowlist` (рівень `fast`) та `build`, `e2e` (додаються на `full`).

## Коли який рівень

| Команда | Що запускає | Коли |
| --- | --- | --- |
| `npm run verify` | рівень `fast` — усе, що не потребує браузера, демона чи мережі | після кожної зміни; ціль — секунди |
| `npm run verify:full` | `fast` + `build` + `e2e` | перед передачею людині й перед checkpoint |
| `npm run verify:checkpoint` | `full` із `--no-skip`: пропуск стає провалом | момент запису checkpoint |

**Запускай `npm run verify` сам, після пачки правок — не чекаючи гейта.** Гейт на `Stop`
дасть той самий вердикт, але вже наприкінці ходу, коли лагодити дорожче.

**Точковий перезапуск** — коли лагодиш один рядок і не хочеш чекати на решту:

```
npm run verify -- --only typecheck
npm run verify -- --only lint,no-secrets
```

`--only` звіряється з рівнем: `--only build` під `fast` — це помилка, а не порожній
прогін (`build` і `e2e` живуть на `full`). Порожня вибірка ніколи не читається як «усе
пройшло».

**`verify:full` — ворота готовності.** Це та команда, зеленість якої дозволяє сказати
людині «готово».

CI в цьому проєкті немає й не передбачено («localhost only, loopback only, nothing
deployed»), тож роль «моменту, коли пропуск недопустимий» відіграє checkpoint. Під
`--no-skip` у запис не може потрапити зелене, під яким половина не бігла.

## Сьогодні `verify:checkpoint` виходить кодом 1 — і це його робота, а не дефект

`tests/e2e/` порожня — e2e-тести належать B-07, не цьому шару, — тож рядок `e2e` чесно
каже `SKIPPED — 0 тестів написано`, а `--no-skip` так само чесно відмовляється назвати
таку передачу повністю перевіреною.

**Полагодити тут нічого.** Щойно тести B-07 з'являться **і проходитимуть**, обидві
команди стануть зеленими без жодної правки коду шару. Про самі ненаписані тести шар
нічого наперед не обіцяє: написані й червоні дадуть `FAILED`, і це теж його робота.

Якщо `verify:checkpoint` червоний з **іншої** або **додаткової** причини — це вже не цей
випадок, і розбиратися треба з причиною.

## П'ять статусів — і що робити з кожним

Спека шару (§3.2) називає п'ять, і жоден не зводиться до іншого:

| Статус | Значення | Блокує | Дія |
| --- | --- | --- | --- |
| `PASSED` | Виконалася, проблем не знайшла | ні | нічого. Але читай «Чого шар не доводить» перед тим, як назвати це доказом |
| `FAILED` | Передумова була, перевірка виконалася, знайшла проблему | так | лагодь код. Повний текст помилки — `npm run verify`, не вердикт гейта |
| `SKIPPED` | Передумови справді не було (немає браузера, бінарника, 0 тестів). **Про код не говорить нічого** | лише за `--no-skip` | не називай зеленим. Назви вголос, що саме пропущено й чому |
| `NOT_RUN` | Перевірка з `after:` не запускалася, бо впала та, від якої вона залежить | так | лагодь передумову; сам рядок не чіпай |
| `UNRUNNABLE` | Сама перевірка не змогла запуститися: `spawn ENOENT`, код виходу 127, нерозбірний вивід | так | це поломка інструмента, не коду. Не «лагодь» код, якого ніхто не перевіряв |

Розрізнення `FAILED` / `UNRUNNABLE` — **не педантизм**. Повідомити «перевірка впала»,
коли вона ніколи не могла запуститися, означає стверджувати щось про код, якого ніхто не
перевіряв.

Окремо: Playwright, що знайшов нуль тестів, дає `SKIPPED` з причиною «0 тестів
написано», а **не** `PASSED`.

## Як читати звіт

Таблиця друкується в термінал; машинна копія того самого прогону лишається в
`.verify/last-run.json` (файл у `.gitignore`, у передачу не йде).

Два рядки виводу, які легко прочитати неправильно:

- **«Зелено, але НІЧОГО НЕ БІГЛО»** — прогін відтворено з кеша свіжості. Це окреме
  твердження: воно означає, що **нічого не виконувалося**. Гейт у цьому разі не блокує,
  але й не мовчить — і називає `npm run verify` як спосіб отримати справжній прогін.
- **Прогін, у якому всі рядки `SKIPPED`,** гейт вважає відмовою, а не зеленим —
  суворіше за будь-який прапорець.

Периметр свіжості дорівнює **всій передачі**: правка `docs/`, `README` чи самого
матеріалу checkpoint протухлює кеш і купує повний прогін. Компроміс свідомий — гейт, що
може проґавити ключ, гірший за гейт, що думає довше.

## Два хуки

| Подія | Скрипт | Що робить |
| --- | --- | --- |
| `PostToolUse` на `Edit`/`Write` | `.claude/hooks/edit-check.mjs` | дає **lead**, ніколи статус: посеред рефакторингу код законно зламаний, і змушувати чинити файл 1 із 3 було б шкідливо. Канал блокування в цій події існує — неблокування тут вибір, а не обмеження |
| `Stop` | `.claude/hooks/stop-gate.mjs` | **єдине місце шару, де народжується статус.** Запускає `fast`-рівень над деревом, яке назвала подія, і на блокувальному вердикті не дає завершити хід |

Обидва беруть корінь дерева з поля `cwd` вхідної події, а **не** з `$CLAUDE_PROJECT_DIR`:
у git worktree змінна лишається на головному checkout, і гейт перевіряв би не те дерево,
яке редагують. Змінна каже тільки, **де лежить скрипт**.

Таймаути задані явно — `PostToolUse` 120 с, `Stop` 300 с, — бо дефолт command-хука
дорівнює **600 с**, а гейт, що висить десять хвилин, зламаний. Число 60 с, яке ходить
поруч, стосується хуків **типу `agent`**, не command-хуків.

Перші два числа стереже `tests/unit/pinned-prose-values.spec.ts`. Два останні — **ні, і
не можуть**: вони з документації Claude Code, а не з файлу цього репозиторію, тож у разі
її зміни ця фраза застаріє мовчки. Звіряйте їх із документацією, а не з цим рядком.

`Stop` мусить лишатися **більшим** за власний таймаут раннера в гейті
(`RUNNER_TIMEOUT_MS` у `stop-gate.mjs`), і причина сильніша за акуратність: Claude Code
скасовує command-хук, що вперся в таймаут, **відкидаючи його вивід**. Гейт, який чесно
вирішив блокувати, віддав би цей вердикт у порожнечу. Нерівність тримає тест, а не
коментар.

**Увімкнення перевірено формою, але не спрацюванням.** `.claude/settings.json` валідний,
обидві події на місці, шляхи абсолютні й розкриваються, таймаути явні, і кожна команда
справді запускається (перевірено синтетичним входом). Чого це **не** доводить: що Claude
Code ці хуки викликає. Хуки читаються на старті сесії, а перезапустити сесію може лише
людина. Точні кроки живої перевірки — `docs/context/verify-layer.md`, розділ 6.

### Коли гейт блокує хід

У тексті блокування стоїть назва перевірки — «`FAILED typecheck`», — **без тексту того,
що не скомпілювалося**. Це наслідок контракту звіту, а не недогляд: повний текст дає
`npm run verify`. Ціна — одна зайва команда, не хибний вердикт.

### Коли ламається сам гейт, захистом є людина, а не механізм

Зламавшись сам — раннер не запустився, звіт нерозбірний, ключа `reused` немає, — гейт
виходить кодом 0 і друкує `systemMessage`. Документація описує `systemMessage` як
повідомлення **користувачеві**, а для події `Stop` єдиний канал, що доходить до моделі, —
це `decision: block` разом із кодом 2. Другого каналу немає, і переводити власну поломку
гейта на блокування відкинуто свідомо: зламаний гейт не дає моделі предмета для
лагодження, тож це був би livelock без виходу.

Отже: шар боронить від «зелено, хоч насправді зламано». Від «шар мовчить, бо зламався
сам» боронить не він, а людина, яка читає повідомлення.

## Рух перевіряється візуально

Комірка `blindSpot` рядка `e2e` виконує пряму вимогу SPRINT-01: «Рух цього тижня
перевіряється візуально; автоматична перевірка руху з керованим часом - завдання релізу
R3, і в записі checkpoint це так і записується». **Жоден рядок реєстру на рух не
дивиться**, і це не прогалина реалізації, а межа, узгоджена на цей спринт.

## Чого цей шар не доводить

Розділ обов'язковий: шар, який не називає власних сліпих плям, сам стає тим, від чого мав
би берегти. **Читай перед тим, як назвати роботу перевіреною.**

- **Нічого візуального.** Сіра карта, картка, що роз'їхалася, значок під неправильним
  кутом — усе це проходить кожен рядок реєстру. SPRINT-01 покладає це на очі.
- **Нічого про рух.** Свідомо, до R3.
- **Нічого про формати з ТЗ**, поки хтось не напише unit-тест на кожен. Тека
  `tests/unit/` не порожня, але живуть у ній тести **самого шару перевірки**: рядок
  `unit` зелений за них, а не за формати картки.
- **Нічого про судження моделі.** Findings Клода лишаються lead: хук їх не рахує й не
  може перевірити. Це припис у тексті блокування, а не механізм.
- **Нічого про те, чого немає в реєстрі.** Реєстр — межа того, що шар узагалі бачить.
- **Не доводить, що написані тести виконалися.** `test.only` за невимкненого
  `forbidOnly` або суцільний `test.skip` дають код виходу 0, і рядки `unit`/`e2e`
  показують `PASSED` за прогін, у якому не виконано жодного твердження (відтворено, не
  виведено з конфігу).
- **Не доводить, що `blindSpot` каже правду.** Ці тексти **рецензовані, не перевірені**:
  жоден тест не спіймав би комірку, яка обіцяє більше, ніж ловить перевірка.
- **Не доводить, що зелена сюїта стереже.** Зелений тест доводить, що перевірка
  **проходить**, а не що вона **вартує**. За час побудови шару мутаційне тестування
  знайшло шість тестів, які мали назву варти, а вартували щось сусіднє; кожен був
  зелений і кожен виглядав доречно. **Наслідок для роботи: кожну нову варту доводь
  мутацією — зламай те, що вона нібито стереже, і переконайся, що вона червоніє.**
- **Нічого про те, що Claude Code справді вантажить ці хуки** — див. «Два хуки» вище.
- **Не бачить `.claude/settings.local.json`.** Файл у `.gitignore`, тож перелік
  переданих файлів його не повертає й хеш свіжості його не бачить: правка в ньому лишає
  закешоване зелене на вигляд чинним.

Повніший перелік — із вимірами й причиною, чому кожну межу лишили, — у
`docs/context/verify-layer.md`, розділ 8.

## Чого не робити

- **Не «лагодь» червоний `verify:checkpoint`,** поки єдина його причина — порожня
  `tests/e2e/`. Див. вище.
- **Не додавай другого тестового раннера.** Юніт-тести виконує Playwright Test окремим
  проєктом `unit` без браузера; критерій B-07 «інших тестових фреймворків немає» лишається
  виконаним. `deps-allowlist` провалить будь-який незнайомий пакет.
- **Не називай зеленим прогін із `SKIPPED`.** «Зелено, але X пропущено» — інше
  твердження, ніж «зелено», і звучати воно має інакше.
- **Не правь `run.mjs`, щоб додати перевірку.** Реєстр — дані.
- **Не запускай `npx playwright test` без `--project`:** dev-сервер піднімається для
  будь-якої вибірки, що може містити `e2e`, і його падіння прочитається як провал
  юніт-тесту.

## Куди далі

| Треба | Де |
| --- | --- |
| Дослівні тексти `proves` / `blindSpot` кожного рядка | `docs/context/verify-layer.md`, розділ 2 |
| Матеріал для запису checkpoint | `docs/context/verify-layer.md`, розділи 5–8 |
| Кроки живої перевірки хуків | `docs/context/verify-layer.md`, розділ 6 |
| Повний перелік названих меж шару | `docs/context/verify-layer.md`, розділ 8 |
| Спека шару | `docs/superpowers/specs/2026-09-15-verify-layer-design.md` |
